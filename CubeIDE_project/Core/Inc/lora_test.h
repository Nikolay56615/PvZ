// lora_test.h
#ifndef LORA_TEST_H
#define LORA_TEST_H

#include <stdint.h>
#include <stdbool.h>

/*
 * Минималистичная библиотека для проверки работы LoRa-модуля с STM32.
 * Файл предназначен для простого теста: отправки фиксированной строки по радио.
 * Не требует очередей, построителей сообщений или сложной логики.
 */

// ==========================
// Инициализация (если требуется)
// ==========================
// Обычно ничего не нужно, так как инициализация GPIO/UART делается в main.c,
// через MX_GPIO_Init(), MX_LPUART1_UART_Init() и т.д.

// ==========================
// Функция ожидания готовности LoRa (AUX = 1).
// timeout_ms — максимальное время ожидания в миллисекундах.
// Возвращает true, если готовность наступила за отведённое время.
bool wait_lora_ready(uint32_t timeout_ms);

// ==========================
// Функция для отправки фиксированной текстовой строки по LoRa-модулю
// (используется низкоуровневый HAL_UART_Transmit)
void send_lora_message(void);

#endif // LORA_TEST_H
