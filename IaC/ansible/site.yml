---
- name: Deploy Mosquitto cluster with dynamic security
  hosts: mosquitto_brokers
  become: true
  vars:
    mosquitto_cluster_name: "pvz-mqtt-cluster"
  roles:
    - mosquitto_cluster
# - name: Deploy PostgreSQL for other services
#   hosts: postgresql_servers
#   become: true
#   roles:
#     - postgresql
